name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    uses: rarebit-one/.github/.github/workflows/reusable-gem-release.yml@v1
    with:
      gem-name: standard_health
      ruby-version: "3.4.4"
    permissions:
      contents: write
      id-token: write
